# Copy to .env and fill in. .env is gitignored.

# Signs Payload session tokens. Generate a fresh one:
#   openssl rand -base64 32
PAYLOAD_SECRET=

# Absolute or relative path to the CivikStack site checkout. The catalogue
# mounts it READ-ONLY to read component source.
SITE_PATH=../civikstack-foundation-site
